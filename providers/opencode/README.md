# OpenCode overlay pending

OpenCode has conflicting roots today:

- ~/.config/opencode/skill
- ~/.config/opencode/agent
- ~/.opencode/skills

Do not repoint OpenCode until one root is made authoritative and old agentctl-* entries are removed.
