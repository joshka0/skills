interface:
  display_name: "Quality Gate"
  short_description: "Gate with reviewer synthesis, CodeRabbit cleanup, and final verification"
  default_prompt: "Use $quality-gate to run the reviewer gate, fix actionable issues, loop on CodeRabbit prompt-only review until clean, run final app checks, and return approved, approved-with-known-risks, or needs-changes."

policy:
  allow_implicit_invocation: true
