---
name: design-family
description: >-
  Router for design skills. Covers: frontend-design, critique, audit, harden,
  polish, adapt, animate, bolder, clarify, colorize, delight, distill, extract,
  normalize, onboard, optimize, quieter, teach-impeccable, emil-design-eng, and
  userinterface-wiki.
---

# Design Family

Use this as the small entrypoint for design work.

## Rule

Pick the design job first, then read only the matching skill from
`references/skill-map.md`. Do not stack multiple design skills unless the user
explicitly asks for a pass sequence.

## Routing

1. Build or redesign a surface -> use `frontend-design`.
2. Review existing UI -> use `critique` or `audit`.
3. Fix last-mile quality -> use `polish` or `harden`.
4. Focus one design axis -> use the relevant micro-skill.

## Pass Order

When a full design pass is requested, use this order:

1. `critique`
2. `frontend-design`
3. `harden`
4. `polish`

Use visual micro-skills only when the critique identifies that specific need.
